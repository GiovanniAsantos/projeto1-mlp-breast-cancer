venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
runs/
checkpoints/*.pt

# Claude Code / agent skills (privado, não vai pro repositório)
.claude/
.agents/
skills-lock.json

# Relatório -- arquivo de trabalho pra gerar o PDF (Claude web); conteúdo real
# já fica na célula final do notebook, que vai pro git
RELATORIO.md
